meta:
  name: test-cluster
  environment: test-env
  region: test-region
  description: |
    Test cluster

spec:
  versionMajor: v0.10
  bootstrapAddrs:
    - bootstrap-addr:9092
  zkAddrs:
    - zk-addr:2181
  zkPrefix: "/test-cluster-id"
  zkLockPath: /topicctl/locks
